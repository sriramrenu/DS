{
    "name": "datasprint-3.0",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "build": "echo 'Root build started' && cd backend && npm run vercel-build && cd ../frontend && npm run build"
    }
}